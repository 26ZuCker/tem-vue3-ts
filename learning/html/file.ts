/**
 * Buffer，注意已被弃用，建议使用以下三者代替
 */
const buffer = new Buffer();

/**
 * Blob
 */
const blob = new Blob();

/**
 * ArrayBuffer
 */
const arraybuffer = new ArrayBuffer();

/**
 * File
 */
const file = new File();

/**
 * FileReader
 */
const fileReader = new FileReader();
