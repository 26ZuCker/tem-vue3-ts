import {
  ref,
  reactive,
  toRefs,
  isRef,
  readonly,
  computed,
  watch,
  watchEffect,
  onMounted,
  onBeforeUnmount,
  SetupContext,
} from 'vue';

interface VirListProps {}
/**
 * 入口函数
 */
const VirListHook = () => {
  return {};
};

/**
 * 类型
 */
export { VirListHook, VirListProps, SetupContext };
